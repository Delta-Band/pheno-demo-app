import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    accentColor: 'red',
    text: '#000'
  },
  typography: {
    fontSize: 16
  }
});

export default theme;
